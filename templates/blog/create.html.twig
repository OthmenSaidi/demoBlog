{% extends "base.html.twig" %}

{% block title %}
création d'un nouvel article

{% endblock %}

{% block body %}
<h1 class="display-4 text-center my-5">
{% if editMode %}
  Modification de l'article n: {{editMode}}
{% else %}

création d'un nouvel article
{% endif %}

</h1>

{# <form method="post" class="col-md-5 mx-auto">
  <div class="form-group">
    <label for="title">Title</label>
    <input type="text" class="form-control" id="title" name="title">
    
  </div>
  <div class="form-group">
    <label for="content">Contenu de l'article</label>
    <textarea type="text" class="form-control" id="content" name="content" rows="15"></textarea>
  </div>
   <div class="form-group">
    <label for="image">Photo de l'article</label>
    <input type="text" class="form-control" id="image" name="image">
    
  </div>
  
  <button type="submit" class="btn btn-secondary mb-5">Enregistrer l'article</button>
</form> #}
 {# {{form(formArticle)}} #}

 {{form_start(formArticle, {'attr': {'class': 'col-md-5 mx-auto'} }) }}
 {{form_row(formArticle.categorie, {'attr': {'placeholder': "choisir une catégorie", "maxlength": false, "pattern":false},
                                          'label': "catégorie de l'article" })}}

 {{form_row(formArticle.title, {'attr': {'placeholder': "saisir le titre de l'article", "maxlength": false, "pattern":false},
                                          'label': "Titre de l'article" })}}

 {{form_row(formArticle.content, {'attr': {'placeholder': "saisir le contenu de l'article",
                                            'rows': 13},
                                          'label': "contenu de l'article"})}}

 {{form_row(formArticle.image, {'attr': {'placeholder': "saisir l'URL de l'image"},
                                          'label': "URL de l'image"})}}

 <button type="submit" class="btn btn-secondary">
 
 {% if editMode %}
  Enregitrer les modifications
{% else %}
Enregistrer l'article
{% endif %}
</button>



 {{form_end(formArticle)}}

{% endblock %}


